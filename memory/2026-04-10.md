# 2026-04-10 — Daily Log

## PR #5215 Follow-up Review (from late 04-09, ~20:00 PDT)
- **PR:** Fixes multi-GPU device resolution in AppLauncher by @kellyguo11
- Kelly pushed commit `6263ff08` ("fix: address review comments")
- **All 4 previous issues addressed:**
  - 🔴 `WORLD_SIZE` vs `device_count` bug → changed to `local_rank < num_visible_gpus`
  - 🔴 Same bug in `sim_launcher.py` → fixed + cleaned up log message
  - 🟡 PresetCfg comment → added "resolved during environment construction"
  - 🟢 Double-write comment → added intentional overwrite explanation
- **Bonus:** Added `TestResolveDistributedDeviceMultiNode` test class (3 test cases)
- **No review posted** — silence = approval per bot policy
- PR ready to merge once CI passes

## PR Review Bot Status
- Inline comments fix confirmed working (tested on PR #5220 and #5215)
- Bot correctly posts comments on specific lines in the diff view
- Safety rules in place across all agent files
# 2026-04-10 — Daily Log

## PR #5215 Follow-up Review (from late 04-09, ~20:00 PDT)
- **PR:** Fixes multi-GPU device resolution in AppLauncher by @kellyguo11
- Kelly pushed commit `6263ff08` ("fix: address review comments")
- **All 4 previous issues addressed:**
  - 🔴 `WORLD_SIZE` vs `device_count` bug → changed to `local_rank < num_visible_gpus`
  - 🔴 Same bug in `sim_launcher.py` → fixed + cleaned up log message
  - 🟡 PresetCfg comment → added "resolved during environment construction"
  - 🟢 Double-write comment → added intentional overwrite explanation
- **Bonus:** Added `TestResolveDistributedDeviceMultiNode` test class (3 test cases)
- **No review posted** — silence = approval per bot policy
- PR ready to merge once CI passes

## PR Review Bot Status
- Inline comments fix confirmed working (tested on PR #5220 and #5215)
- Bot correctly posts comments on specific lines in the diff view
- Safety rules in place across all agent files

## ⚠️ Kelly Report: Reviews Posted as Her Account (08:12)
- Kelly reported seeing reviews posted with her personal account `kellyguo11` instead of `isaaclab-review-bot[bot]`
- Checked PR #5215 and #5220 — both show reviews from `isaaclab-review-bot[bot]` (not kellyguo11)
- Asked Kelly which specific PR she saw reviews from her account on
- **CRITICAL**: All reviews MUST use bot token, never personal account
- Need to investigate if there's a code path where `gh` CLI uses default auth instead of exported `GH_TOKEN`
# 2026-04-10 — Daily Log

## PR #5215 Follow-up Review (from late 04-09, ~20:00 PDT)
- **PR:** Fixes multi-GPU device resolution in AppLauncher by @kellyguo11
- Kelly pushed commit `6263ff08` ("fix: address review comments")
- **All 4 previous issues addressed:**
  - 🔴 `WORLD_SIZE` vs `device_count` bug → changed to `local_rank < num_visible_gpus`
  - 🔴 Same bug in `sim_launcher.py` → fixed + cleaned up log message
  - 🟡 PresetCfg comment → added "resolved during environment construction"
  - 🟢 Double-write comment → added intentional overwrite explanation
- **Bonus:** Added `TestResolveDistributedDeviceMultiNode` test class (3 test cases)
- **No review posted** — silence = approval per bot policy
- PR ready to merge once CI passes

## PR Review Bot Status
- Inline comments fix confirmed working (tested on PR #5220 and #5215)
- Bot correctly posts comments on specific lines in the diff view
- Safety rules in place across all agent files

## 🚨 Reviews Posted as kellyguo11 on Arena Repo (08:16)
- Kelly reported reviews posted with her personal account on `isaac-sim/isaaclab-arena`
- **Root cause:** Bot GitHub App has `repository_selection: selected` — only installed on specific repos
- `isaaclab-arena` is NOT in the bot's installation, so bot token fails and sub-agents fall back to default `gh` auth (Kelly's personal account)
- **Found reviews on arena PRs posted as kellyguo11:**
  - PR #5 (Adds an initial Arena environment) — reviews 4085856227, 4085881568
  - Other arena PRs likely affected too

### Fix Required
1. Add `isaaclab-arena` to bot's GitHub App installation:
   - Go to: https://github.com/organizations/isaac-sim/settings/installations
   - Find `isaaclab-review-bot`, click Configure
   - Under "Repository access", add `isaaclab-arena`
2. Offered to add safeguard: abort if identity check fails instead of falling back to personal auth

### Technical Details
- Bot installation ID: 68523893
- Current repos: `repository_selection: selected` (not all repos)
- Bot token works for IsaacLab main repo, fails silently for arena
# 2026-04-10 — Daily Log

## PR #5215 Follow-up Review (from late 04-09, ~20:00 PDT)
- **PR:** Fixes multi-GPU device resolution in AppLauncher by @kellyguo11
- Kelly pushed commit `6263ff08` ("fix: address review comments")
- **All 4 previous issues addressed** — no review posted (silence = approval)
- PR ready to merge once CI passes

## PR Review Bot Status
- Inline comments fix confirmed working (tested on PR #5220 and #5215)
- Bot correctly posts comments on specific lines in the diff view
- Safety rules in place across all agent files

## 🚨 Reviews Posted as kellyguo11 on Arena Repo (08:16)
- Kelly reported reviews posted with her personal account on `isaac-sim/isaaclab-arena`
- **Root cause:** Bot GitHub App only installed on specific repos, not arena
- **Fix:** Add `isaaclab-arena` to bot's GitHub App installation at https://github.com/organizations/isaac-sim/settings/installations

## PR #5178 Review (09:40)
- **PR:** Fix Pausing and Unpausing in KIT GUI by @garylvov
- **SHA:** `bc97e18b`
- **Review ID:** 4091155075 (posted as `isaaclab-review-bot[bot]`)
- **Result:** Clean, no issues found
- Fix adds `wait_for_playing()` to block training loop during GUI pause
- Adds `_re_sync_fabric()` to fix articulation visualization freeze on resume
- Follows existing patterns, already approved by @AntoineRichard
- Good to merge

## PR #5177 Review (10:02)
- **PR:** Simplify preset resolution with single-pass resolve_presets by @kellyguo11
- Review spawned, awaiting completion

## PR #5177 Review Complete (10:10)
- **PR:** Simplify preset resolution with single-pass resolve_presets by @kellyguo11
- **SHA:** `a980f9de`
- **Result:** No blocking issues
- Main concern (infinite loop on unresolvable chained PresetCfg) was fixed — now raises `ValueError` with cycle detection via `seen: set[int]`
- Added 4 new test cases for error paths
- Minor observations (non-deterministic set iteration, duplicated CLI parsing) remain but non-blocking
- CI failures unrelated (sensor/camera rendering tests)

## PR #5179 Naming Discussion (10:09)
- Kelly asked about naming `BaseXformPrimView` — needs name for non-physics reference frame (USD XformPrim / sites)
- Existing patterns: `ArticulationBase`, `AssetBase`, `SensorBase`, `RigidObjectBase`
- **Recommendation: `FrameBase`** — concise, matches robotics terminology, encompasses both USD XformPrim and MuJoCo "site" concepts
- Alternative: `XformBase` (matches USD terminology directly)
