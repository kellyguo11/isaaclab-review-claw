# 2026-03-25 — Isaac Lab Full Training Campaign

## Project: Isaac Lab RL Training Validation

### Goal
1. Generate new pretrained checkpoints for all Isaac Lab environments on latest `develop` branch
2. Validate all environments still train as expected after quaternion ordering changes
3. Compare against v2.3.2 benchmark thresholds for regression detection
4. Fix any broken environments and submit PRs with before/after proof
5. Upload checkpoints to accessible location (GitHub Releases on kellyguo11/IsaacLab fork)

### Setup Completed
- Isaac Lab on `develop` branch (commit 7a0cb834de8) at `/home/horde/IsaacLab`
- Isaac Sim 6.0.0 (built from source) linked via `_isaac_sim` symlink
- Python 3.12 venv at `env_isaaclab/` with all RL frameworks:
  - rsl_rl (5.0.1), rl_games (1.6.1), skrl (1.4.3), sb3 (2.7.1)
  - PyTorch 2.10.0+cu128 on NVIDIA L40 GPU
- Training campaign script: `training_campaign/run_training.py`
- TensorBoard running on http://135.225.53.55:6006

### Training Campaign
- **156 total jobs** across 4 RL frameworks (rsl_rl, rl_games, skrl, sb3)
- Prioritized: rsl_rl first (49 tasks), then rl_games (37), skrl (43), sb3 (7), camera last (20)
- Using benchmark config thresholds from `configs.yaml` `full` mode
- Progress tracked in `training_campaign/progress_develop.json`
- Results saved to `training_campaign/results_develop.json`

### v2.3.2 Baseline
- Cannot run v2.3.2 directly — it needs Isaac Sim 5.1.0 (not available on pip, our build is 6.0.0)
- Using benchmark thresholds in configs.yaml as the reference baseline
- Thresholds were set based on expected v2.3.2 performance

### Early Results
- Isaac-Ant-Direct-v0 (rsl_rl, 300 iter): reward=5963, threshold=7000 — **below threshold** (170s)
  - Still climbing at iter 300, may need more iterations

### Key Decisions
- Fork for PRs: kellyguo11/IsaacLab
- Checkpoint upload: GitHub Releases (gh auth on kellyguo11)
- Skip Showcase/Deploy/AMP/IK/Mimic/ROS tasks (not standard RL training)
- Include all camera/vision tasks (appended at end of queue)

### External Access
- TensorBoard: http://135.225.53.55:6006
- Public IP: 135.225.53.55
